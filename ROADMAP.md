# Roadmap

## Phase 1 — 看清楚自己（已完成）

- [x] 只读网关：富途 OpenAPI + 离线 Mock
- [x] SQLite 增量同步（成交 90 天切片、断点续传、按日快照）
- [x] FIFO 归因：完整交易、胜率、盈亏比、持有期
- [x] 组合结构：权重、HHI/有效持仓数、市场与币种暴露
- [x] 风险指标：净值曲线、最大回撤、波动率、Sharpe/Sortino
- [x] 规则化风险观察（集中度、处置效应、数据缺口）
- [x] CLI + 本地 Web 面板

## Phase 2 — 说出来

### 2.1 雪球仓位播报

把仓位变动自动写成文章发到雪球。

- 变动检测：比较相邻两个 `position_snapshots`，产出结构化 diff
  （新开仓 / 清仓 / 加仓 / 减仓 / 权重漂移），带阈值过滤掉噪声
- 文案生成：按**本人写作风格**生成中文正文（先沉淀一份 style profile，
  可以喂几篇自己写过的帖子做参考），模板 + LLM 两条路都留口子
- 发布：走 Chrome 自动化打开雪球并使用已登录会话发帖
  （不在项目里存任何雪球账号密码；登录态复用浏览器 profile）
- 安全阀：默认 **dry-run**，先把草稿写到 `reports/`，人工确认后再发；
  发布记录落库，避免重复推送同一次变动

模块规划：`src/ftrade/publish/{diff.py, writer.py, xueqiu.py}`。

### 2.2 建议引擎

- `AdviceEngine` 接口已在 `advice/engine.py` 预留
- 把 Phase 1 的 report 交给 LLM，产出中文建议正文
- 补齐 TWR：需要现金流（出入金）记录，先把资金流水同步进来

## Phase 3 — 想更远

- 基准对比（恒指 / 标普 / 自定义组合）与超额收益归因
- 行业 / 因子暴露
- 定投与再平衡的模拟（仍然只做模拟，不下单）
